{
  "name": "wu-web",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "Vite + TS frontend for the half-body dance workbench. Lives alongside pose_viewer.html during Phase 1 migration.",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "preview": "vite preview --port 4200",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@types/node": "^25.6.0",
    "@types/three": "^0.184.1",
    "jsdom": "^29.1.1",
    "typescript": "^5.6.0",
    "vite": "^7.0.0",
    "vitest": "^2.0.0"
  },
  "dependencies": {
    "@mediapipe/tasks-vision": "^0.10.35",
    "three": "^0.184.0"
  }
}
